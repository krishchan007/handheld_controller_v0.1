This folder preserves the original sample IP5306 library code that was added to the project before the refactor.

Files here are kept for reference only and are intentionally outside the active PlatformIO `src/` and `lib/` build paths.

Current active implementation:
- `lib/IP5306/src/IP5306.h`
- `lib/IP5306/src/IP5306.cpp`
